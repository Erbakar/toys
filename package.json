{
  "name": "toys",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "npm install --prefix object-detector && npm run build --prefix object-detector"
  }
}
